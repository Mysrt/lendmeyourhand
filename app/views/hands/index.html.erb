<h1>Listing hands</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Photo</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @hands.each do |hand| %>
  <tr>
    <td><%= hand.name %></td>
    <td><%= hand.photo_id %></td>
    <td><%= link_to 'Show', hand %></td>
    <td><%= link_to 'Edit', edit_hand_path(hand) %></td>
    <td><%= link_to 'Destroy', hand, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Hand', new_hand_path %>
